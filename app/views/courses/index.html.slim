h1 コース 一覧
= link_to "新規追加" , {controller: "courses", action: "new"}, class: "btn btn-default"
.row
  table.table.table-bordered.table-striped.default_datatables
    thead
      tr
        th 会社名
        th コース名
        th 担当者
        th 発注書
        th 書籍送付
        th 出席票作成
        th 修了証
        th 開始日
        th 終了日
        th メモ
    tbody
      - @courses.each do |b|
        tr
          td= link_to b.company.client_name, {controller: "companies", action: "edit", id: b.company_id}
          td
            = link_to b.name,{action: "show", id: b.id}
            = link_to "(edit)", edit_course_path(b.id)
          td = b.user.name if b.user.present?
          td= check_course_img(b, "order_flg")
          td= check_course_img(b, "book_flg")
          td= check_course_img(b, "attendee_table_flg")
          td= check_course_img(b, "diploma_flg")
          td= b.getStartDate
          td= b.getEndDate
          td
            - if b.memo.present?
              span.glyphicon.glyphicon-paperclip data-toggle="tooltip" title="#{b.memo}" 
