<!DOCTYPE html>
<html>
<head>
  <title>社内顧客DB</title>
  <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
    <%= wicked_pdf_stylesheet_link_tag "pdf" -%>
    <%= wicked_pdf_javascript_include_tag "number_pages" %>
</head>
<body>

  <style>
      body{
        text-align:center;
        margin:0;
        padding:0;
        font-size:13px;
        background-color:#fff;
        -webkit-print-color-adjust: exact;
        color:#000; }

      .wrapper{margin:0 auto; width:730px;}

      .content{
        padding:0px 10px 10px;
        text-align: left;
        background-color:#FFFFFF;
        padding-bottom:25px;
        position: relative;
      }


      .in{width:850px; margin:25px auto; text-align:left;}

      .line{border-top:1px solid #bab9b9; height:1px; }



      /*.............................................................General.................... */

      h1, h2, h3 {margin:0; padding:0; }

      h1{font-size:32px;text-align: center;padding: 15px 0 40px;}

      h2{font-family:Georgia; font-size:24px; font-style:italic; }

      #invoiceHead {
        padding-bottom: 5px;
      }

      .alpha {
        float:left;
        text-align: left;
        width : 350px
      }

      .beta {
        float:right;
        text-align:left;
        font-size: 14px;
        margin-right: 5px;
        width: 270px;
      }

      .gamma {
        float:right;
        height:80px;
        width:70px;
        overflow: hidden;
      }

      .recipientName {
        font-size : 150%;
        margin-bottom:50px;
        text-decoration: underline;
      }

      .titleName {
        font-size : 130%;
        margin-bottom:50px;
      }

      .senderName {
        font-size : 120%;
      }

      .alnright {
        text-align:right;
      }
      /*.............................................................Table................... */
      dl.totalAmountPrice {
        width: 80%;
        border-bottom : 1px solid #000;
        font-size: 130%
      }

      .totalAmountPrice dt {
        float:left;
      }
      .totalAmountPrice dd {
        float:right;
        text-align: right;
      }

      td { padding: 12px; text-align:left; }

      .gray {background-color:#CCC;}

      .table_main {  
        border-collapse: collapse;
        width: 100%;
        margin-bottom: 50px;
      
      }

      .table_main th {border: 1px solid #000; text-align: center;font-size:14px;padding: 3px;}
      .table_main tr {height: 20px;}
      .table_main td {border: 1px solid #000;padding:10px}
      .table_main td.blank {border:none;}

      #footerLogo {
        width:100px;
        position:absolute;
        bottom:0cm;
        right:0.3cm;
      }
      .page-break { display:block; clear:both; page-break-after:always; }

      /* for modern brouser */
      .clearfix:after {
        content: ".";
        display: block;
        clear: both;
        height: 0;
        visibility: hidden;
      }
      /* IE7,MacIE5 */
      .clearfix {
        display: inline-block;
      }
      /* WinIE6 below, Exclude MacIE5 \*/
      * html .clearfix {
        height: 1%;
      }
      .clearfix {
        display: block;
      }
  </style>
<%= yield %>

</body>
</html>
